# NG Starter Kit

To use this:

* `git clone https://github.com/xmlking/ng-starter-kit.git`
* `cd ng-starter-kit`
* `npm install -g jspm gulp`
* `npm install`
* `gulp serve`
* `http://localhost:3000`
